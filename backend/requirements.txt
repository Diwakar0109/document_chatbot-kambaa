# --- FINAL REQUIREMENTS FOR RENDER (v5) ---
# Updating pandas to a version with a pre-compiled wheel for Python 3.13.

# Web Framework & Server
fastapi==0.111.0
uvicorn==0.29.0
gunicorn==22.0.0

# Environment & Utilities
python-dotenv==1.0.1
python-multipart==0.0.9

# LangChain Core & Integrations
langchain==0.2.1
langchain-core==0.2.3
langchain-community==0.2.1
langchain-groq==0.1.5
langchain-cohere==0.1.7

# Vector Store
faiss-cpu==1.9.0.post1

# Document Parsers (with updated pandas)
pypdf
python-docx
pandas
openpyxl

# Fix for initial PyYAML build errors
PyYAML==6.0.1

# Build tools
setuptools
wheel
cython
